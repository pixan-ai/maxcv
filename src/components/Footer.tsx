export default function Footer() {
  return (
    <footer className="w-full border-t border-[--ink-100]">
      <div className="max-w-2xl mx-auto px-4 py-6 text-center text-xs text-[--ink-500]">
        <span className="font-[family-name:var(--font-mono)]">maxcv</span>
        {" — "}
        free for everyone, forever.
      </div>
    </footer>
  );
}
